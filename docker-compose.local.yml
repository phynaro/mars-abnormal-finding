services:
  # Backend service with local build and hot reload
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: mars-backend:local
    container_name: mars-backend-local
    command: npm run dev
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=development
      - REDIS_URL=redis://redis:6379
      # Ensure file watcher uses polling (needed for bind mounts on Windows)
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - redis
    volumes:
      # Mount source code for development (hot reload)
      - ./backend/src:/app/src
      - ./backend/swagger:/app/swagger
      # Nodemon config (polling) so file changes are detected in Docker on Windows
      - ./backend/nodemon.json:/app/nodemon.json
      # Persistent uploads directory
      - ./backend/uploads:/app/uploads
      # Mount logs directory for debugging
      - ./backend/logs:/app/logs
      # Preserve node_modules (anonymous volume)
      - /app/node_modules
    # No port mapping - only accessible through nginx proxy
    networks:
      - mars-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/api/health"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Notification worker (BullMQ consumer for create-ticket etc.)
  notification-worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: mars-backend:local
    container_name: mars-notification-worker-local
    command: node src/workers/notificationWorker.js
    env_file:
      - ./backend/.env
    environment:
      - NODE_ENV=development
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./backend/src:/app/src
      - /app/node_modules
    networks:
      - mars-network
    depends_on:
      - redis
    restart: unless-stopped

  # Frontend service with Vite dev server and hot reload
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    image: mars-frontend:local
    container_name: mars-frontend-local
    command: npm run dev
    environment:
      - NODE_ENV=development
      - VITE_API_URL=/api
      - VITE_LIFF_ID=2008351084-nRdr4kE4
    volumes:
      # Mount source code for development (hot reload)
      - ./frontend:/app
      - /app/node_modules
    # No port mapping - only accessible through nginx proxy
    networks:
      - mars-network
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Nginx reverse proxy for local development
  nginx:
    image: nginx:alpine
    container_name: mars-nginx-local
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # Use local nginx configuration
      - ./nginx-local-dev.conf:/etc/nginx/nginx.conf:ro
      # SSL certificates (optional)
      # - ./ssl:/etc/ssl:ro
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_healthy
    networks:
      - mars-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Database service for local development
  # Uncomment if you need a local database
  # db:
  #   image: mcr.microsoft.com/mssql/server:2022-latest
  #   container_name: mars-db-local
  #   environment:
  #     - ACCEPT_EULA=Y
  #     - SA_PASSWORD=YourStrong@Passw0rd
  #     - MSSQL_PID=Developer
  #   ports:
  #     - "1433:1433"
  #   volumes:
  #     - mars_db_data:/var/opt/mssql
  #   networks:
  #     - mars-network
  #   restart: unless-stopped

  # Redis for BullMQ notification queue
  redis:
    image: redis:7-alpine
    container_name: mars-redis-local
    ports:
      - "6379:6379"
    volumes:
      - mars_redis_data:/data
    networks:
      - mars-network
    restart: unless-stopped
    command: redis-server --appendonly yes

networks:
  mars-network:
    driver: bridge
    name: mars-local-network

volumes:
  # Uncomment if using database
  # mars_db_data:
  #   driver: local
  #   name: mars-db-data-local
  mars_redis_data:
    name: mars-redis-data-local
