version: '3.8'

services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - VITE_LIFF_ID=2007697113-EWn7vw08
      - VITE_API_URL=https://4eec5e8fde9e.ngrok-free.app/api
    restart: unless-stopped
    networks:
      - mars-network

networks:
  mars-network:
    driver: bridge
