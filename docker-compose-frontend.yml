version: '3.8'

services:
  frontend:
    build: 
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://96f6316bf908.ngrok-free.app/api
        - VITE_LIFF_ID=2007697113-EWn7vw08
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - VITE_LIFF_ID=2007697113-EWn7vw08
      - VITE_API_URL=https://96f6316bf908.ngrok-free.app/api
    restart: unless-stopped
    networks:
      - mars-network

networks:
  mars-network:
    driver: bridge
